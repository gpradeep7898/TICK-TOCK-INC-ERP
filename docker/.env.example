# ─── Tick Tock Inc. ERP — Docker environment ─────────────────────────────────
# Copy this file to .env (same docker/ directory) and fill in real values.
# Never commit the real .env to version control.

# ── PostgreSQL ────────────────────────────────────────────────────────────────
# Password for the postgres superuser inside the container.
POSTGRES_PASSWORD=changeme

# ── API ───────────────────────────────────────────────────────────────────────
# Must be a long, random string. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace-me-with-a-long-random-secret

# Token lifetime (e.g. 8h, 24h, 7d)
JWT_EXPIRES_IN=8h

# Application version shown in /health
APP_VERSION=1.0.0

# Set to production (disables verbose error details)
NODE_ENV=production

# Host port the API listens on (mapped to container port 3001)
HOST_PORT=3001
